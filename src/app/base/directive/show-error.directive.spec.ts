import { ShowErrorDirective } from './show-error.directive';

describe('ShowErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowErrorDirective();
    expect(directive).toBeTruthy();
  });
});
